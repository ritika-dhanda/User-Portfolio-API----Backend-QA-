{
  "name": "user-portfolio-api",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/server.js",
   "test": "cross-env NODE_ENV=test jest"

  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^29.0.0",
    "supertest": "^6.0.0"
  }
}
